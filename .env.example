# Gemini API Key for AI features (parsing tasks, project breakdown, smart priorities)
GEMINI_API_KEY=

# Supabase client configuration (publishable key only; never expose a secret/service-role key)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
